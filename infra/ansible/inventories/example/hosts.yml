all:
  children:
    observability:
      hosts:
        node-a:
          # The podman observability stack runs on this Mac. Use ansible_connection
          # local because the playbook executes on the observability host itself
          # (same machine that runs observability/up.sh).
          ansible_connection: local
          ansible_host: 192.168.1.64
