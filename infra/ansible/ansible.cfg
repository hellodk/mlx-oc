[defaults]
inventory = inventories/example/hosts.yml
host_key_checking = False
retry_files_enabled = False
deprecation_warnings = False
# Many-node rings deploy in parallel (the ring setup is per-node independent
# until the final start). 32 forks handles a 20+ node buildout comfortably.
forks = 32
